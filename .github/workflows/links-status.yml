name: Links Status

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

env:
  TZ: Asia/Shanghai

jobs:
  links-status:
    runs-on: ubuntu-latest

    steps:
    - name: Pull Latest Repository
      uses: actions/checkout@v4

    - name: Cache pip packages
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Ensure temp folder is not empty
      run: |
        mkdir -p ./temp
        echo "placeholder" > ./temp/.keep

    - name: Download temp file artifact
      uses: dawidd6/action-download-artifact@v6
      with:
        github_token: ${{secrets.GITHUB_TOKEN}}
        branch: main
        name: 'temp'
        path: './temp'
        if_no_artifact_found: warn

    - name: Download existing JSON files from page branch
      run: |
        # 从 page 分支下载现有的 JSON 文件作为备份
        curl -sL "https://raw.githubusercontent.com/${{ github.repository }}/page/all.json" -o all.json.backup || echo "No existing all.json found"
        curl -sL "https://raw.githubusercontent.com/${{ github.repository }}/page/errors.json" -o errors.json.backup || echo "No existing errors.json found"
        curl -sL "https://raw.githubusercontent.com/${{ github.repository }}/page/status.json" -o status.json.backup || echo "No existing status.json found"
        curl -sL "https://raw.githubusercontent.com/${{ github.repository }}/page/cache.json" -o ./temp/cache.json.backup || echo "No existing cache.json found"

    - name: Check RSS feeds
      env:
        FCL_REPO: ${{ github.repository }}
      continue-on-error: true
      run: |
        echo "Checking RSS feeds..."
        python run.py || echo "Python script completed with errors, continuing..."
        echo "Generated files:"
        ls -la *.json || echo "No JSON files generated"
        echo "Temp folder:"
        ls -la ./temp || echo "Temp folder not found"
    
    - name: Upload temp file as artifact
      uses: actions/upload-artifact@v4
      with:
        name: 'temp'
        path: './temp'
        retention-days: 30

    - name: git config
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'

    - name: Commit changes
      run: |
        mkdir pages
        # 检查文件是否存在，不存在则使用备份文件
        if [ ! -f all.json ]; then
          if [ -f all.json.backup ]; then
            mv all.json.backup all.json
          else
            echo '{"article_data":[],"statistical_data":{"friends_num":0,"active_num":0,"error_num":0,"article_num":0,"last_updated_time":""}}' > all.json
          fi
        fi
        if [ ! -f errors.json ]; then
          if [ -f errors.json.backup ]; then
            mv errors.json.backup errors.json
          else
            echo '[]' > errors.json
          fi
        fi
        if [ ! -f status.json ]; then
          if [ -f status.json.backup ]; then
            mv status.json.backup status.json
          else
            echo '{"total_count":0,"accessible_count":0,"inaccessible_count":0,"last_updated":"","results":[]}' > status.json
          fi
        fi
        if [ ! -f ./temp/cache.json ]; then
          if [ -f ./temp/cache.json.backup ]; then
            mv ./temp/cache.json.backup ./temp/cache.json
          else
            echo '[]' > ./temp/cache.json
          fi
        fi
        cp -r ./static/index.html ./static/favicon.ico ./static/edgeone.json ./temp/cache.json all.json errors.json status.json pages/
        cd pages
        git init
        git add .
        git commit -m "$(date +"%Y年%m月%d日-%H时%M分") GitHub Actions定时更新"
        git push --force https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:page

    - name: Trigger Tencent Cloud Pages Webhook
      run: |
        echo "Triggering Tencent Cloud Pages webhook..."
        max_retries=3
        retry_count=0
        success=false
        
        while [ $retry_count -lt $max_retries ]; do
          retry_count=$((retry_count + 1))
          echo "Attempt $retry_count of $max_retries..."
          
          response=$(curl -s -w "\n%{http_code}" -X POST "https://pages-api.cloud.tencent.com/v1/webhook/3c204cf9de1a98d4366d36a8d3f5937f5462b39b53025d2e34edd33021b3e6ec" \
            -H "Content-Type: application/json" \
            -d '{"message": "GitHub Actions build completed"}')
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "HTTP Status: $http_code"
          echo "Response: $body"
          
          if [ "$http_code" = "200" ]; then
            code=$(echo "$body" | grep -o '"code":[0-9]*' | head -1 | cut -d':' -f2)
            if [ "$code" = "0" ]; then
              echo "Webhook triggered successfully!"
              success=true
              break
            else
              echo "API returned error code: $code"
            fi
          else
            echo "HTTP request failed with status: $http_code"
          fi
          
          if [ $retry_count -lt $max_retries ]; then
            echo "Retrying in 3 seconds..."
            sleep 3
          fi
        done
        
        if [ "$success" = "false" ]; then
          echo "::error::Webhook trigger failed after $max_retries attempts"
          exit 1
        fi

    - name: Delete Workflow Runs
      uses: Mattraks/delete-workflow-runs@v2
      with:
        retain_days: 30
        keep_minimum_runs: 6

  keepalive-workflow:
    name: Keepalive Workflow
    if: ${{ always() }}
    needs: links-status
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - uses: liskin/gh-workflow-keepalive@v1
